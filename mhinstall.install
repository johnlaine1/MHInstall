<?php
/**
 * @file
 * Installation and schema hooks for mhinstall.module.
 */

/**
 * Implements hook_schema().
 */
function mhinstall_schema() {
  $schema = array();
  // The table for the mhinstall entity type.
  $schema['mhinstall_mhinstall'] = array(
    'description' => 'The base table for the MH Install entity',
    'fields' => array(
      'id' => array(
        'description' => 'Primary key of the MH Install entity',
        'type' => 'serial',
        'unsigned' => TRUE,
        'not null' => TRUE,
      ),
      'label' => array(
        'description' => 'Human readable label for a MH Install',
        'type' => 'varchar',
        'length' => '255',
        'not null' => TRUE,
      ),
      'created' => array(
        'description' => 'Timestamp of when the MH Install was created',
        'type' => 'int',
        'length' => 10,
        'not null' => FALSE,
      ),
      'updated' => array(
        'description' => 'Timestamp of when the MH Install was last updated',
        'type' => 'int',
        'length' => 10,
        'not null' => FALSE,
      ),
    ),
    'primary key' => array('id'),
  );
  return $schema;
}
